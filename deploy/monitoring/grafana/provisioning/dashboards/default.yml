apiVersion: 1

providers:
  - name: DOM XSS local observability
    orgId: 1
    folder: DOM XSS
    type: file
    disableDeletion: true
    editable: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
